// Generated by CoffeeScript 2.0.1
(function() {
  // Place all the behaviors and hooks related to the matching controller here.
  // All this logic will automatically be available in application.js.
  // You can use CoffeeScript in this file: http://coffeescript.org/

  // $(document).ready ->
  //   age_adult = $("#age_adult")
  //   $('#age_adult').keyup ->
  //     current_value = $.trim @value
  //     if current_value is ''
  //       preview.hide().attr 'src', ''
  //     else
  //       preview.show().attr 'src', current_value

  // box = document.querySelector "#age_adult"
  // label = document.querySelector 'label[for="test1"]'

  // numberListener =(event) ->
  //   label.textContent = box.value

  // box.addEventListener 'onkeyup', numberListener
  var box, myHandler;

  box = null;

  myHandler = function() {
    var label, numberListener;
    box = document.querySelector("#age_adult");
    label = document.querySelector('label[for="test1"]');
    numberListener = function(event) {};
    return label.textContent = box.value;
  };

  document.addEventListener("DOMContentLoaded", myHandler, false);

}).call(this);
